syntax = "proto3";

option go_package = "github.com/ehsaniara/scheduler";

package com.github.ehsaniara;

message Task{
  string taskUuid = 1;
  Type taskType = 2;
  map<string, bytes> header = 3;
  bytes pyload = 4;

  enum Type{
    PUB_SUB = 0;
    SHELL_CMD = 1;
  }
}
